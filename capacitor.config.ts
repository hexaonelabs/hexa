import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dworkspace.app',
  appName: 'd-workspace',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
